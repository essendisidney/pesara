import { AdminPlaceholder } from "@/components/admin/shell";

export default function Page() {
  return (
    <AdminPlaceholder title="Ventures">
      Accepted ventures receive a workspace. Nothing is published to /portfolio
      unless the relationship is real and visibility is on.
    </AdminPlaceholder>
  );
}
