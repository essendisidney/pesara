import { AdminPlaceholder } from "@/components/admin/shell";

export default function Page() {
  return (
    <AdminPlaceholder title="Settings">
      Metric visibility, assessment weights, and roles. Audit history cannot be
      rewritten by ordinary admins.
    </AdminPlaceholder>
  );
}
