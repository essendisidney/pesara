import { AdminPlaceholder } from "@/components/admin/shell";

export default function Page() {
  return (
    <AdminPlaceholder title="Analytics">
      Screening time, validation time, stage conversion. Sensitive application
      contents are not captured in analytics events.
    </AdminPlaceholder>
  );
}
