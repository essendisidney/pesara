import { AdminPlaceholder } from "@/components/admin/shell";

export default function Page() {
  return (
    <AdminPlaceholder title="Pipeline">
      Funnel by stage, country, industry and source. Conversion rates appear
      when volume exists.
    </AdminPlaceholder>
  );
}
