import { AdminPlaceholder } from "@/components/admin/shell";

export default function Page() {
  return (
    <AdminPlaceholder title="Founders">
      CRM for contact, applications, meetings and internal notes. Internal notes
      never appear on founder screens.
    </AdminPlaceholder>
  );
}
